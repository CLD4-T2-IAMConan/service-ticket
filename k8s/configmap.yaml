apiVersion: v1
kind: ConfigMap
metadata:
  name: ticketservice-config
data:
  db.host: "postgres-service"
  db.port: "5432"
  db.name: "ticketservice_db"
  application.yml: |
    spring:
      application:
        name: ticketservice-service
      jpa:
        hibernate:
          ddl-auto: update
        show-sql: true
