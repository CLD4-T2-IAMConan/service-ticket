apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-ticket.fullname" . }}
  labels:
    {{- include "service-ticket.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service-ticket.selectorLabels" . | nindent 4 }}
